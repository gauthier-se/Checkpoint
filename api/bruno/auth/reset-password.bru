meta {
  name: Reset Password
  type: http
  seq: 11
}

post {
  url: {{baseUrl}}/api/auth/reset-password
  body: json
  auth: none
}

body:json {
  {
    "token": "REPLACE_WITH_UUID_TOKEN_FROM_LOGS",
    "newPassword": "new-secure-password123"
  }
}
